继承和派生

继承：定义新类B拥有已有类A所有特性，可将类A作为基类，B作为A的派生类（子类）

派生类可以通过对基类进行修改和扩充得到
拥有基类所有成员函数和变量
在派生类成员函数中，不能访问基类的private成员 

class 派生类名：public 基类名
{
};

派生类内存空间：
	派生类的体积=基类体积+派生类对象自己的成员变量的体积
	基类对象存储位置在派生类新增变量之前

在派生类中定义与基类同名同参数表的函数，是对基类成员函数的覆盖，在覆盖函数中，可以通过  基类名::积累成员函数()  的方式调用基类原本的成员函数。

