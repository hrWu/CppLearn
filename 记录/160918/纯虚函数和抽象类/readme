纯虚函数和抽象类

纯虚函数：没有函数体的虚函数
virtual void Print() = 0;//纯虚函数

抽象类：包含纯虚函数的类即是抽象类
	1 只能作为 基类 来派生新类使用
	2 不能创建抽象类的对象
	3 抽象类的指针和引用->有派生类派生出来的类的对象

抽象类中：
	1 在成员函数内可以调用纯虚函数（实际上是多态：Print() <=> this->Print(),此时是基类指针指向派生类对象）
	2 在构造函数/析构函数内部不能调用纯虚函数

如果一个类从抽象类派生而来
	只用实现积累中所有纯虚函数，才能成为非抽象类！！
