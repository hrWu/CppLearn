虚析构函数

问题的提出：
	base* p;
	p = new Dedrived;
	delete p;
此时的delete语句只会对基类的析构函数进行调用，而不会调用派生类的析构函数。

虚析构函数可以实现首先对派生类的析构函数进行析构，再对基类的析构函数调用析构。

解决方法：
	把基类的析构函数声明为virtual
	1 派生类的析构函数virtual可以不进行声明
	2 通过基类的指针删除派生类对象时
		首先调用派生类的析构函数
		然后调用基类的析构函数
	类如果定义了虚函数，则最好将析构函数也定义成虚函数！

注意：不允许虚函数作为构造函数！
