 基类和派生类有同名成员

 使用派生类成员变量：i=5
 使用派生类成员函数：func()

 使用基类成员变量：base::i=5//base=>基类名称
 使用基类成员函数：base::func()


 存储空间 ：基类成员存储空间在派生类之前
派生类函数不能访问基类私有成员变量



访问范围说明符：
private
public
见图片
protected：
基类的pretected成员:可以被以下函数访问：
	基类的成员函数
	基类的友元函数
	派生类的成员函数可以访问当前对象的基类的保护成员（注意，不能访问其他基类对象的保护成员）
