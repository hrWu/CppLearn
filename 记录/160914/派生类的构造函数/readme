派生类的构造函数

派生类对象包含基类对象
执行派生类构造函数之前，先执行基类的构造函数

构造函数名(形参表):基类名(集类构造函数实参表)//需要使用初始化列表

派生类调用基类构造函数的两种方式：
	显式方式
	派生类的构造函数中->积累的构造函数提供参数
	派生类::派生类构造函数名(形参表)::基类构造函数名(基类构造函数形参表){}
	隐式方式
	派生类的构造函数中，省略集类构造函数时，派生类的构造函数，自动调用基类的默认构造函数
派生类的析构函数首先被执行，执行完派生类的析构函数后，自动调用基类的析构函数

包含类的对象的派生类：
	执行派生类的构造函数之前：
	初始化列表中：
	1 调用基类的构造函数	->初始化派生类对象从基类继承的成员
	2 调用成员对象的构造函数->初始化派生类对象中的成员对象

	执行完派生类的析构函数后：
	1 调用成员对象类的析构函数
	2 调用基类的析构函数

